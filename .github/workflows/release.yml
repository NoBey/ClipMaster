name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest

    steps:
      # 1. 检出代码
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2. 提取版本号
      - name: Extract Version
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing version: $VERSION"

      # 3. 构建应用
      - name: Build Release
        run: |
          chmod +x build_app.sh
          VERSION=${{ steps.version.outputs.version }} ./build_app.sh

      # 4. 创建分发包
      - name: Create Archive
        run: |
          cd build
          zip -r ClipMaster-${{ steps.version.outputs.version }}.zip ClipMaster.app

      # 5. 创建 GitHub Release
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: ClipMaster v${{ steps.version.outputs.version }}
          body: |
            ## 🚀 ClipMaster ${{ steps.version.outputs.version }}

            ### 📥 下载说明
            1. 下载 `ClipMaster-${{ steps.version.outputs.version }}.zip`
            2. 解压缩
            3. 双击运行 ClipMaster.app
            4. 在系统偏好设置中授权必要的权限

            ### ⚙️ 系统要求
            - macOS 13.0 或更高版本
            - Apple Silicon (M1/M2/M3/M4)

            ### ✨ 主要功能
            - 自动剪切板监听和记录
            - 菜单栏快速访问
            - 实时搜索和过滤
            - 智能分类识别
            - 隐私保护（应用黑名单）
            - 置顶和收藏功能

            ### 📝 变更日志
            查看完整变更历史：https://github.com/NoBey/ClipMaster/commits/main
          files: |
            build/ClipMaster-${{ steps.version.outputs.version }}.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
